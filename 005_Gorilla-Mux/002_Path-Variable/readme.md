Program: URL Path Variable

Purpose: to define variables for URL Path
- format: {name} or {name:pattern}
- if a regular expression pattern is not defined, the matched variable will be anything until the next slash.